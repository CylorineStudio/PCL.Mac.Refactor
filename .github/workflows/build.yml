name: Build App

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-26
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Build app
        run: |
          xcodebuild \
          -project PCL.Mac.xcodeproj -scheme PCL.Mac -configuration Release \
          ARCHS="arm64 x86_64" ONLY_ACTIVE_ARCH=NO \
          CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO \
          -archivePath build archive

      - name: Prepare app artifact directory
        run: |
          mkdir "PCL.Mac"
          mv build.xcarchive/Products/Applications/PCL.Mac.app "./PCL.Mac/PCL.Mac.app"
          mv build.xcarchive/dSYMs/PCL.Mac.app.dSYM ./PCL.Mac/PCL.Mac.app.dSYM

      - name: Upload app artifact
        uses: actions/upload-artifact@v4
        with:
          name: PCL.Mac-${{ github.sha.substr(0,7) }}
          path: ./PCL.Mac
